// Generated by SharpEmu Gen5MslTranslator (experimental spike).
#include <metal_stdlib>

using namespace metal;

kernel void gen5_cs(
    uint gen5_lane [[thread_index_in_simdgroup]])
{
    const uint gen5_lane_mask = 1u << gen5_lane;
    uint s0 = 0x00000000u;
    uint s126 = 0xFFFFFFFFu; // exec_lo, all lanes on
    uint v1 = 0u;
    uint v2 = 0u;
    uint v5 = 0u;

    // pc=0x0000 VMulLoU32 v5 <- v1, v2, s0
    if ((s126 & gen5_lane_mask) != 0u)
    {
        v5 = v1 * v2;
    }
    // pc=0x0008 VMulHiU32 v5 <- v1, v2, s0
    if ((s126 & gen5_lane_mask) != 0u)
    {
        v5 = mulhi(v1, v2);
    }
    // pc=0x0010 VMulLoI32 v5 <- v1, v2, s0
    if ((s126 & gen5_lane_mask) != 0u)
    {
        v5 = v1 * v2;
    }
    // pc=0x0018 VMulHiI32 v5 <- v1, v2, s0
    if ((s126 & gen5_lane_mask) != 0u)
    {
        v5 = as_type<uint>(mulhi(as_type<int>(v1), as_type<int>(v2)));
    }
    // pc=0x0020 SEndpgm
    return;
}
